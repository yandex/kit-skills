{
  "title": "Ответы для CTA-полосы",
  "type": "object",
  "properties": {
    "titleText": {
      "type": "string",
      "title": "Заголовок (призыв)"
    },
    "subtitleText": {
      "type": "string",
      "title": "Подзаголовок (необязательно)"
    },
    "buttonText": {
      "type": "string",
      "title": "Текст кнопки"
    },
    "buttonUrl": {
      "$ref": "#/definitions/LinkSource",
      "title": "Ссылка: объект {link, linkId} (linkId можно пустым)"
    },
    "palette": {
      "type": "string",
      "title": "Палитра (id из настроек магазина)"
    },
    "verticalRhythm": {
      "type": "string",
      "title": "Вертикальный отступ блока (spacing-токен, например 500 = 40px с каждой стороны)"
    },
    "verticalRhythmTouch": {
      "type": "string",
      "title": "Вертикальный отступ на телефоне (spacing-токен)"
    }
  },
  "required": [
    "titleText",
    "buttonText"
  ]
}
