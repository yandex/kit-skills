"""Internal library for the kit-storefront-constructor fixed-route client.

`scripts/kit.py` stays the single entry point; these modules only factor its
schema engine, workspace editing, and write core into reviewable units.
"""
