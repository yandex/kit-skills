# selected-custom-snippets — превью

Сетка карточек избранных товаров пользователя (favourite-products-controller) со своим оформлением карточки и кнопкой-сердцем. Схема пропатчена импортёром: в оригинале доки html читает dataSource и count, которых в схеме не было (add-missing-datasource, add-missing-count).
