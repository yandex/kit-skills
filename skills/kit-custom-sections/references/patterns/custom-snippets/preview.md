# custom-snippets — превью

Замена листинга категории (страница listing): панель фильтров в выезжающем drawer (поэтому в css есть :global(.drawer …) — контент drawer рендерится порталом вне скоупа секции, это осознанно), сортировка, сетка карточек со своим оформлением (фото, цены, кнопка в корзину со степпером), пагинация. Все данные — из products-listing-controller; настроек почти нет.
