# hero — превью

Первый экран: две колонки (на мобильной — одна, текст сверху). Слева вертикальный стек с воздухом (gap 300): заголовок h1 с собственной шкалой **52px/1.08 на десктопе, 34px на мобильной** (заметно крупнее 24px заголовков рядовых секций — это и есть иерархия страницы), подзаголовок xl вторичным цветом (если задан), ниже крупная primary-кнопка (ссылку можно передать в answers объектом {link, linkId} или задать в форме). Справа изображение 4:3, object-fit cover, скругление 200 — объект из kit.py media upload передаётся в answers. Все цвета — токены палитры магазина, hex-хардкода нет: секция слушается смены темы.
