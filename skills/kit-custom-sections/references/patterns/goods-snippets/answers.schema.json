{
  "title": "Ответы для полки с кастомными сниппетами",
  "type": "object",
  "properties": {
    "titleText": {
      "type": "string",
      "title": "Заголовок секции"
    },
    "titleAlign": {
      "type": "string",
      "enum": [
        "default",
        "left",
        "center"
      ],
      "title": "Расположение заголовка"
    },
    "palette": {
      "type": "string",
      "title": "Палитра (id из настроек магазина)"
    },
    "count": {
      "type": "number",
      "title": "Сколько товаров"
    },
    "columns": {
      "type": "number",
      "title": "Колонок на десктопе"
    },
    "dataSource": {
      "$ref": "#/definitions/ProductsSource",
      "title": "Источник товаров: объект {id, slug, type} категории/подборки магазина"
    },
    "verticalRhythm": {
      "type": "string",
      "title": "Вертикальный отступ блока (spacing-токен, например 500 = 40px с каждой стороны)"
    },
    "verticalRhythmTouch": {
      "type": "string",
      "title": "Вертикальный отступ на телефоне (spacing-токен)"
    }
  },
  "required": [
    "titleText"
  ]
}
